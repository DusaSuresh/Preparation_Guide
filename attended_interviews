AT&T Senior Engineer - Round 1
--------------------------

- Java8 futures list down and write the code.
- Records feature in Java14

write a program to print the name of the feature with feature characteristic name qualifier and value GOOD
let str = 
    "feature": [
    {
      "name": "Test1",
      "featureCharacteristic": [
        {
          "name": "priority",
          "valueType": "string",
          "value": "2"
        },
        {
          "name": "qualifier",
          "valueType": "string",
          "value": "GOOD"
        }
      ]
    },
    {
      "name": "Test2",
      "featureCharacteristic": [
        {
          "name": "priority",
          "valueType": "string",
          "value": "1"
        },
        {
          "name": "qualifier",
          "valueType": "string",
          "value": "BEST"
        }
      ]
    }
  ]
};


class Feature{
    private String name;
    private List<FeatureCharacterstic> featureCharacterstic;
    Feature(name, featureCharacteristic){
        this.name = name;
        this.featureCharacteristic = featureCharacteristic;
    }

}
class FeatureCharacterstic{
    private String name;
    private String valueType;
    private value;
    FeatureCharacterstic(name, valueType, value){
        this.name = name;
        this.valueType = valueType;
        this.value = value;
    }
}

FeatureCharacterstic featureCharacteristic1 = new FeatureCharacterstic("priority", "String", "2");
FeatureCharacterstic featureCharacteristic2 = new FeatureCharacterstic("qualifier", "String", "GOOD");
FeatureCharacterstic featureCharacteristic3 = new FeatureCharacterstic("priority", "String", "1");
FeatureCharacterstic featureCharacteristic4 = new FeatureCharacterstic("qualifier", "valueType", "BEST");

List<FeatureCharacterstic> list1 = new ArrayList<>();
list1.add(featureCharacteristic1);
list1.add(featureCharacteristic2);
Feature feature1 = new Feature("Test1", list1);


List<FeatureCharacterstic> list2 = new ArrayList<>();
list1.add(featureCharacteristic3);
list1.add(featureCharacteristic4);
Feature feature1 = new Feature("Test2", list2);

List<Feature> featureList = new ArrayList<>();


for(int i=0; i<feature.size(); i++){
    Feature feature = featureList.get(i);
    for(int j=0; j<feature.featureCharacteristic.size(); j++){
        FeatureCharacterstic featureChar = feature.featureCharacteristic.get(j);
        if(featureChar.name.equals("qualifier") && featureChar.value.equals("GOOD")){
            System.out.println(feature.get(i).name);
        }
    }
}


===============================================================================================

AT&T Senior Engineer - Round 2
--------------------------
- how can you filter duplicate employee objects from list
- CompletableFeature for multiple service/api calls.
- east/west deployments.
- functional programming. can we access the variable outside of class
- how do you define apis for cart add, update, delete.
- apache kafka, how it works. did you use it in project.
- how do you make sure api is available, regarding promothesus, grafana. Scaling of apis
- Security in apis

===============================================================================================
AT&T Director round(Final)

- Thread dump --> how do you analyze it.
- Jmiter --> what parameters you will look into it.
- what is test case ?
- PCI data security how did you handle?
- if you get any production defect, how do you handle it.? what is the process of analysis.
- you have given code which has performance drawback and very hard to maintain. what strategies you will take as engineer to rewrite it.
- As I am fullstack developer but have PMP, SAFeAgile certificates. why these have or do I need to remove it from resume.
- One of API has latency and you have given task to improve the performance of it. What will you check and improve it.

===================================================================================================

AT&T Lead full stack -- Round 1:
----------------------------

- Streams in react js while making api call instead of waiting.
- core web vitals
- Promoise methods ==> all allSettled
- how will you identify the re-renders.
- useState(prev -> prev+1) how previous state we have to get and why
- rateLimit working. what happens when bulk request posted to endpoint. How can we prevent. How the rate limit works and implementation.
- how the webchunks will be created, what are confiugrations for code splitting.
- node memory managment in apis.

=====================================================================================================

AT&T Hackerrank - 11/11/25
Question 1 — NodeJS: ExpressJS Middleware Basics
✅ Problem

Create an Express middleware that:

Adds a trace object to every /tasks route request.

The trace object contains:

id → unique ID (string)

timestamp → epoch time (UTC)

path → request path (without query params)

Sets the header x-request-id with the same ID.

Mount middleware only for /tasks routes.

Use shortid for generating unique IDs.

Export middleware as default (ES module syntax).

✅ Solution
middleware.js
import shortid from "shortid";

const middleware = (req, res, next) => {
  const trace = {
    id: shortid.generate(),
    timestamp: Date.now(),
    path: req.path,
  };

  req.trace = trace;
  res.setHeader("x-request-id", trace.id);
  next();
};

export default middleware;

app.js
import express from "express";
import middleware from "./middleware.js";
import tasksRouter from "./routes/tasks.js";
import indexRouter from "./routes/index.js";

const app = express();

// Only for /tasks routes
app.use("/tasks", middleware, tasksRouter);

// Other routes remain normal
app.use("/", indexRouter);

export default app;


✅ Why it works

ES module syntax (import/export default) — compatible with package.json having "type": "module".

Middleware attached only for /tasks.

Adds x-request-id header and trace object properly.

🧩 Question 2 — React: Timer Component
✅ Problem

Create a React Timer component that:

Starts automatically from a given initial value (prop).

Decrements every second.

Stops automatically at 0.

Has a Stop Timer button to stop manually.

Has these data-testid attributes:

timer-value (show current value)

stop-button (stop the timer)

✅ Solution
Timer.jsx
import React, { useEffect, useState } from "react";

const Timer = ({ initial }) => {
  const [time, setTime] = useState(initial);
  const [isRunning, setIsRunning] = useState(true);

  useEffect(() => {
    if (!isRunning) return;

    const timerId = setInterval(() => {
      setTime((prev) => {
        if (prev > 0) return prev - 1;
        clearInterval(timerId);
        return 0;
      });
    }, 1000);

    return () => clearInterval(timerId);
  }, [isRunning]);

  const stopTimer = () => setIsRunning(false);

  return (
    <div>
      <h1 data-testid="timer-value">{time}</h1>
      <button data-testid="stop-button" onClick={stopTimer}>
        Stop Timer
      </button>
    </div>
  );
};

export default Timer;


✅ Why it works

Uses useState for current value and useEffect for countdown.

clearInterval ensures cleanup.

Stops automatically at 0 or manually on button click.

Compatible with Jest + React Testing Library tests using jest.useFakeTimers().

🧩 Question 3 — Java Interface: Building, School, Hospital
✅ Problem

Implement Building interface with:

void floorCompleted(int floorNumber);
void printStatus(int floorNumber);
void printNumberOfFloors();

===================================================================================
